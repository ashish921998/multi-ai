# Browser app environment.
# Copy to .env.local and fill in from your Convex project.
# After running `npx convex dev`, this is shown as the deployment URL.
VITE_CONVEX_URL=https://your-deployment.convex.cloud
